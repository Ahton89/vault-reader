{
  "name": "vault-reader",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run fix:format && npm run build:package",
    "build:package": "ncc build index.js -o dist",
    "fix:format": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@vercel/ncc": "^0.38.1",
    "concurrently": "^9.0.1",
    "js-yaml": "^4.1.0",
    "node-vault": "^0.10.2",
    "prettier": "^3.3.3"
  }
}
